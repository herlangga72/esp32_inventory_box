#include "MailboxSystem.h"

// All implementation is in ServiceRegistry.cpp.
// This file exists only for backward compatibility with the build system.
// MailboxSystem methods are inline in the header and delegate to g_registry.
